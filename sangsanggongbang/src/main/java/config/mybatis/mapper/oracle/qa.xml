<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gr.ssgb.qa.model.QaDAO">
	 
	<insert id="insertQa" parameterType="qaVo">
		<selectKey keyProperty="no" resultType="int" order="BEFORE">
			select qa_seq.nextval from dual
		</selectKey>
		insert into qa(qa_no, c_no, u_no, qa_title, qa_content, qa_step, qa_groupid, qa_regdate)
		values(#{qaNo}, #{cNo}, #{uNo}, #{qaTitle}, #{qaContent}, #{qaStep}, #{qaGroupid}, #{qaRegdate})
	
	</insert>
	
	<select id="selectQaAll" resultType="qaVo">
		select count(*)from qa where qa_no=#{qaNo}
	</select>
</mapper>
	

