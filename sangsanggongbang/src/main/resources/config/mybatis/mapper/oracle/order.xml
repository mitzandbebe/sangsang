<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gr.ssgb.order.model.OrderDAO">
	<insert id="insertOrder" parameterType="orderVo">
		<selectKey keyProperty="orderNo" resultType="int" order="BEFORE">
			select orders_seq.nextval from dual 
		</selectKey>
		insert into orders(order_id, pay_type, c_no, pay_id, m_id, pay_date, price, card_no, refund, ea, order_no, imp_uid, merchant_uid)
		values(#{orderId}, #{payType}, #{cNo}, #{payId}, 
			#{mId},#{payDate}, #{price}, #{cardNo}, #{refund}, #{ea}, #{orderNo}, #{impUid}, #{merchantUid})
	</insert>
	
	<!-- <select id="selectOrdersView" parameterType="int" resultType="map">
		select * from orders
		where orderno=#{orderNo}
	</select> -->
</mapper>









